import 'package:flutter/material.dart';

class Gps extends StatelessWidget {
  const Gps({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('GPS'));
  }
}
